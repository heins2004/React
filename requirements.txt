# Python dependencies for the Django backend (server/)
# Install from project root: pip install -r requirements.txt
# Or from server folder:  cd server && pip install -r requirements.txt

Django==4.2.16
gunicorn==22.0.0
whitenoise==6.7.0
dj-database-url==2.2.0
psycopg2-binary==2.9.9
cloudinary==1.41.0
django-cloudinary-storage==0.3.0
